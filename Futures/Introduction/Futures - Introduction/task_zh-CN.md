## 异步 Rust
##
线程并不是在 Rust 中编写并发程序的唯一方式。\
在本章中，我们将探讨另一种方法：**异步编程**。

具体来说，您将学习以下内容：

- 使用 `async`/`.await` 关键字，轻松编写异步代码
- `Future` 特性，用于表示可能尚未完成的计算
- `tokio`，运行异步代码最流行的运行时
- Rust 异步模型的协作性质，以及它如何影响您的代码

<br/>

## 任务
入门任务是完成 `intro` 函数。  
该函数应返回字符串：***I'm ready to learn about futures!***